import random

def generate_hidden_grid(rows, columns, num_fish)
    grid = []

    for row_num in range(rows):
        row = []
        for colum_num in range (colum):
            row.append("-")
        grid.append(row)



  
